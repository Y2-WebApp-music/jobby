export type AchievementItem = {
  id: number;
  name: string;
  from: string;
  description: string;
  skills: string[];
  images: string[];
  date: string;
};

export type ApplicationItem = {
  id: number;
  title: string;
  company: string;
  note: string;
};
